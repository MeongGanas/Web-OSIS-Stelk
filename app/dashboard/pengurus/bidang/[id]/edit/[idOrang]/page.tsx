import { EditAnggotaForm } from "@/app/ui/admin/FormBidang";

export default async function Page({ params }: { params: { id: number } }) {
  return (
    <div className="container pb-20 pt-32">
      {/* <EditAnggotaForm data={}/> */}
    </div>
  );
}
